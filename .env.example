# Copy to .env and fill in. .env is gitignored -- never commit real values.

# Telegram alerting (optional: if unset, alerts are logged only)
# To post into a CHANNEL: create the channel, add this bot as an ADMIN, then set
# TG_CHAT_ID to the channel's @username (public) or its -100... id (private).
# The bot stays the sender; the channel is just the destination.
TG_TOKEN=your-telegram-bot-token
TG_CHAT_ID=your-chat-id-or-@channel

# Detection parameters (optional -- sensible defaults in config)
DEPTH_USD=500000
SPREAD_THRESHOLD_PCT=1.5
VELOCITY_THRESHOLD_PCT_MIN=0.5
FEE_BUFFER_PCT=0.2
CONFIRM_SECONDS=180
MIN_SAMPLES=6
MAX_FETCH_WINDOW_S=2.0
BENCHMARK_TOLERANCE_PCT=0.5
POLL_INTERVAL_S=20
PANEL_PORT=8080
DATA_LOG_PATH=snapshots.csv
